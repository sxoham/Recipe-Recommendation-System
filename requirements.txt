Flask>=2.0,<3
gunicorn>=20.1.0
python-dotenv>=1.0.0

# Common data / ML libs (remove if not needed)
pandas>=1.4.0
numpy>=1.21.0
scikit-learn>=1.0.0
joblib>=1.1.0

# Optional utilities (uncomment if used)
# flask-cors>=3.0.10
# openpyxl>=3.0.0

# If you maintain a requirements file from your env later, regenerate with:
# pip freeze > requirements.txt
